#flatten is used to convert a multi-dimensional array into a one-dimensional array. It returns a copy of the original array collapsed into one dimension.
import numpy as np
arr = np.array([[10,20],[30,40],[50,60]])
print(arr.flatten())