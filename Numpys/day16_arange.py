#np.arange is used to create an array with evenly spaced values within a given interval. It is similar to the built-in range function but returns an array instead of a list.
import numpy as np
arr = np.arange(2,8)
print(arr)