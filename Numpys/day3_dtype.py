#To find the data type of an array, we can use the dtype attribute. The dtype attribute returns the data type of the array elements.
import numpy as np
arr = np.array([10,20,30,40,50,60])
print(arr.dtype)